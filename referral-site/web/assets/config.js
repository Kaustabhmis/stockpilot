/* Paste your PUBLIC Apps Script deployment URL here (the one deployed with
   "Who has access: Anyone"). It ends in /exec — not /dev. */
window.GB_CONFIG = {
  API_URL: 'PASTE_YOUR_PUBLIC_APPS_SCRIPT_EXEC_URL_HERE',

  /* Public base URL of this site, used to build the referral share links.
     Leave blank to derive it from the current page. */
  SITE_URL: ''
};
